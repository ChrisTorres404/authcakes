# --- Required for Migrations/Seeding ---
AUTH_JWT_SECRET=supersecretjwtkeychangeme   # required, can be any string in dev
APP_URL=http://localhost:3030              # required, your app's base URL
SMTP_USER=your-smtp-user                   # required, can be dummy in dev
SMTP_PASSWORD=your-smtp-password           # required, can be dummy in dev
DB_TYPE=postgres                           # required
DB_HOST=localhost                          # required
DB_PORT=5432                               # required
DB_USERNAME=authcakes_user                 # required, match your DB setup
DB_PASSWORD=CupC@k3s4You!                 # required, match your DB setup
DB_NAME=authcakes_test                     # using dedicated test database
DB_SYNCHRONIZE=false                       # recommended for migrations
DB_LOGGING=true                            # enable for test debugging

# --- App ---
APP_PORT=3030
NODE_ENV=test
APP_CORS_ORIGINS=*
APP_BASE_URL=http://localhost:3030

# --- Auth ---
AUTH_JWT_SECRET=supersecretjwtkeychangeme
AUTH_JWT_ACCESS_EXPIRES_IN=900
AUTH_JWT_REFRESH_EXPIRES_IN=604800
AUTH_PASSWORD_BCRYPT_ROUNDS=10
AUTH_PASSWORD_MIN_LENGTH=8
AUTH_PASSWORD_REQUIRE_NUMBERS=false
AUTH_PASSWORD_REQUIRE_SPECIAL=false
AUTH_MFA_ENABLED=false
AUTH_MFA_TOTP_WINDOW=1
AUTH_COOKIE_DOMAIN=
AUTH_COOKIE_SECURE=false
AUTH_COOKIE_SAMESITE=lax
AUTH_SECURITY_MAX_FAILED_ATTEMPTS=5
AUTH_SECURITY_LOCK_DURATION_MINUTES=30

# --- Database ---
DB_TYPE=postgres
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=authcakes_user
DB_PASSWORD=CupC@k3s4You!
DB_NAME=authcakes_test
DB_SYNCHRONIZE=false
DB_LOGGING=true
DB_SSL=false
DB_MIGRATIONS_RUN=true

# --- Application ---
API_PREFIX=api
APP_NAME=AuthCakes
APP_URL=http://localhost:3030
FRONTEND_URL=http://localhost:5173

# --- Email (SMTP) ---
SMTP_HOST=smtp.example.com
SMTP_PORT=587
SMTP_USER=your-smtp-user
SMTP_PASSWORD=your-smtp-password
SMTP_FROM_EMAIL=admin@example.com
SMTP_FROM_NAME=AuthCakes
SMTP_SECURE=false

# --- Rate Limiting ---
THROTTLE_TTL=60
THROTTLE_LIMIT=10
THROTTLE_SKIP=true

# --- Logging ---
LOG_LEVEL=debug